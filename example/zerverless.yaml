apiVersion: zerverless.io/v1
kind: Application
metadata:
  name: flask-example
  namespace: example
spec:
  source:
    repoURL: https://github.com/nemoNoboru/zerverless/example
    branch: main
  
  functions:
    # Flask app as Docker deployment
    - path: /flask-example
      runtime: docker
      dockerfile: ./Dockerfile
      context: .
  
  static:
    dir: ./static

